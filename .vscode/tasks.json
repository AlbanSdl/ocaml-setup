{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${workspaceFolder}/buildAndRun.bat",
            "problemMatcher": [],
            "args": [
                "${workspaceFolder}",
                "ocaml.ml"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}